// en passant — entry point.
// Gallery renders the slides first, then the deck wires up navigation,
// the exhibition shell, and the splash animation over the built DOM.
import './gallery.js';
import './statement.js';
import './deck.js';
